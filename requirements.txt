# Dependencias de Python para la integración SecLLM + IaC-Eval (ruta Ollama).
#
# RECOMENDADO: instalar dentro de un entorno virtual para NO afectar el Python
# del sistema ni otros proyectos (ver README, sección "Instalación"):
#
#   python -m venv .venv
#   .venv\Scripts\activate      (Windows)   |   source .venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
#
# Versiones como mínimos (>=) para que funcione en Python 3.10–3.14.

# --- Pipeline de integración (scripts/pipeline*.py, generate_report*.py) ---
pandas>=1.5
requests>=2.28
PyYAML>=6.0
tabulate>=0.9        # requerido por DataFrame.to_markdown() en generate_report.py
scipy>=1.10          # tests estadísticos de generate_report_v2.py (Wilcoxon, Spearman, ...)

# --- Análisis y figuras (notebooks/analisis_resultados.ipynb) ---
matplotlib>=3.7
jupyter>=1.0

# --- Núcleo de SecLLM cuando se usa Ollama / API compatible con OpenAI ---
openai>=1.30
numpy>=1.24
rich>=13.0
tqdm>=4.64

# --- OPCIONALES (NO necesarios para Ollama) ---
# Descomentar solo si vas a usar:
#   - modelos locales HuggingFace dentro de SecLLM:
# torch
# transformers
# accelerate
#   - la API de Anthropic (Claude) como auditor:
# anthropic
